# ai-services/

Host-seitige Hilfs-Tools für MetaMove.

## Inhalt

| Verzeichnis / Datei | Zweck |
|---|---|
| [metamove_tools/](metamove_tools/) | Python-API für `metamove_bridge`-ROS2-Services über WebSocket (ohne rclpy) |
| `tcp_echo_probe.py` | Netzwerk-Diagnose (TCP-Echo) zum Prüfen der Verbindung |

> Die EGM-Bridge und der EGM-Mock liegen jetzt unter [`bridge/`](../bridge/).
